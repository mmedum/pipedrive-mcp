# Release verification — v1.0.0

**Date:** 2026-09-24
**Commit:** `release/v1.0.0`
**Workspace:** the configured live workspace (a real CRM, not a sandbox)

**Phase 5 go-ahead:** given by the maintainer on 2026-09-24, per the
phase rule in `CLAUDE.md`. Tagging v0.6.0 did not authorize this; the
go-ahead did, and it is recorded here because that is where the rule
says to record it.

## What was exercised

| Check | Result |
| --- | --- |
| `make check` — 15 gates | pass |
| `make licenses` (local toolchain, per the documented exception) | pass |
| `go test -race -count=3 -shuffle=on ./...` | pass, 11 packages |
| Reproducible build, two clean builds | byte-identical SHA256 |
| `make integration-writes` — live reads, guards, dry runs, reversible writes | pass, every restore verified |
| `make evals` × 3 | see below |
| Tool schema diff, `v0.6.0..HEAD` | one breaking change, footer present |
| Vendored mcpb manifest schema, re-fetched | unchanged upstream |
| Vendored MCP registry schema, re-fetched | unchanged upstream |
| Pipedrive v2 OpenAPI mirror, re-captured | spec moved, **no field changed** |
| `/security-review` over `v0.6.0..HEAD` | clean — `audit/security-reviews/v1.0.0.md` |
| `/simplify` per PR | run on every PR in the range |
| **Claude Desktop smoke** | **NOT DONE — see below** |

## Two bugs were found during the release itself

Both after the first round of gates had gone green, and both are in the
record because a release that only lists what passed is not a record.

**`manage_activity` could not link an activity to a person.** Found by
asking what else the moved spec might cover. The mirror above holds
RESPONSE fields only, so checking the REQUEST side — every query
parameter and every write-body field this client sends, against the new
spec — was a separate pass, and it named three fields the spec does not
declare. Two are spec omissions the live suite exercises every run
(`first_name`/`last_name` on persons). The third was real: `person_id`
on an activity is read-only upstream, Pipedrive rejects a body carrying
it, and nothing here had ever set it. Fixed in #92; `person_id` now
travels as the primary participant, and a live probe holds it.

**A tool's guidance on an upstream failure never reached the LLM.**
Found by `/security-review`, which correctly filed it as correctness
rather than a vulnerability. `llmMessage` returned the upstream message
alone and discarded the wrapper, so `search`'s carefully worded
liveness-failure guidance was dead text and the comment claiming
otherwise was false. Fixed in #93.

Neither was caught by `make check`, and neither would have been.

## The upstream mirror was re-captured, and that is the interesting row

`internal/pipedrive/testdata/v2-response-fields.json` holds Pipedrive's
own v2 description as an oracle for this package's hand-written types.
The spec itself moved — SHA256 `8cf6d47d…` to `fc2ce0fe…` since the
2026-09-18 capture — and **not one field on the seven mirrored types
changed**: no field added, none removed, none retyped.

That is worth stating rather than silently updating a hash. The check
exists because this mirror has drifted before, and the last refresh
found three real defects. This one found none, which is only
informative because somebody looked.

## Eval baseline, three runs

12 tasks per run, ~$1.71 each, $5.13 for the three.

| Task | Runs passed |
| --- | --- |
| `refuse-the-overwrite` | **0 / 3** |
| `archiving-is-not-closing` | 2 / 3 |
| the other ten | 3 / 3 |

Four tasks report a half nobody could check — they are reads, so there
is no end state to read back, and they say so rather than quietly
scoring nothing.

No run reported workspace drift, a failed fixture reset, or a record
left behind.

This is the same result as v0.6.0, task for task.

**The runs predate the last two fixes**, which landed after them
(#92 `manage_activity` person_id, #93 hinted errors). They were not
re-run, and the reason is specific rather than convenient: no eval task
creates an activity, so #92 changes no path any run took, and #93
changes the text of a failure that did not occur in any run — the
liveness check succeeded every time. The deterministic gates above were
all re-run on the final commit.

## What this release's evals found, and did not

Nothing new. That is the finding, and it is a weaker one than v0.6.0's
— which caught `manage_deal reopen` having been broken against the real
API since it shipped.

**`refuse-the-overwrite` failing 3/3 is a measurement, not a defect,
and this release is the proof.** v0.6.0 recorded that the model reads
the guard's refusal — which names `overwrite` as the argument that
would permit the write, because a refusal the caller cannot act on is a
bug — and re-sends with it set. It also recorded that rewriting the
field description to say a refusal is NOT a retry signal changed
nothing.

This release made `overwrite` name the fields it may replace, so the
blanket is gone: a model has to be wrong about each field separately,
and the call records which ones it claimed. The task still fails 3/3,
with the model sending `overwrite: ["title"]` — the exact array the
refusal printed.

So the per-field permit did what it was for (the blanket is gone, and
the claim is now specific and recorded) and did not do what it could
never do. Anything a refusal says, a model can echo back. Nothing
in-band prevents this, and the next person to look at this number
should treat it as instrumentation rather than a bug to close.

## Claude Desktop smoke: not done

Open across **five** releases now — v0.4.0, v0.5.0, v0.6.0 and this one
(v0.1.0 predates the requirement). The maintainer was asked before
tagging and chose to ship without it again.

What the automation does reach: `make smoke` against the built binary,
the live integration suite over the server that binary builds, and
three model-driven eval runs against a real workspace. What none of
them reach is the `.mcpb` bundle path — the generated launcher, and
keyring access from a GUI-launched process.

That untested surface is unchanged in size since v0.5.0. What has
changed is the promise over it: this is the release that makes the tool
surface stable, and the bundle is how most people will install it.
